<% @cart_items.each do |cart_item| %>
<table>
  <thead>
    <tr>
      <td>商品画像</td>
      <td>商品名</td>
      <td>個数</td>
      <td>合計金額</td>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><%= image_tag cart_item.item.get_image %></td>
      <td><%= cart_item.item.name %></td>
      <td><%= cart_item.amount %></td>
      <td>¥<%= cart_item.subtotal%></td>
    </tr>
  </tbody>
</table>
<h1>ご購入ありがとうございます</h1>
<P>引き続きお買い物をお楽しみください</P>
<%= link_to '買い物を続ける', public_customers_path, class: 'nav-link text-white' %>
<% end %>